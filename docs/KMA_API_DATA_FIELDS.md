# 기상청 API 데이터 필드 및 API 응답 구조

## 📥 기상청에서 수집하는 데이터

### 1. 단기예보 (get-kma-weather)
**API 엔드포인트**: `https://apihub.kma.go.kr/api/typ02/openApi/VilageFcstInfoService_2.0/getVilageFcst`

#### 수집 데이터 (weather_forecasts 테이블)

| 컬럼명 | 한국어명 | 타입 | 설명 |
|--------|----------|------|------|
| `nx` | 격자 X좌표 | INTEGER | 예보 격자 X 좌표 |
| `ny` | 격자 Y좌표 | INTEGER | 예보 격자 Y 좌표 |
| `base_date` | 발표일자 | TEXT | 예보 발표일 (YYYYMMDD) |
| `base_time` | 발표시각 | TEXT | 예보 발표시각 (HHMM) |
| `fcst_datetime` | 예보일시(UTC) | TIMESTAMPTZ | 예보 시각 (UTC) |
| `fcst_datetime_kr` | 예보일시(KST) | TIMESTAMPTZ | 예보 시각 (KST) |
| `location_code` | 지역코드 | TEXT | 조위 관측소 코드 (DT_XXXX) |
| `한글지역명` | 지역명 | TEXT | 한글 지역명 |
| **기상 요소** |
| `tmp` | 1시간 기온 | NUMERIC | °C |
| `tmn` | 일 최저기온 | NUMERIC | °C |
| `tmx` | 일 최고기온 | NUMERIC | °C |
| `uuu` | 풍속(동서성분) | NUMERIC | m/s |
| `vvv` | 풍속(남북성분) | NUMERIC | m/s |
| `vec` | 풍향 | NUMERIC | deg (풍향 각도) |
| `wsd` | 풍속 | NUMERIC | m/s |
| `sky` | 하늘상태 | NUMERIC | 코드값 (1:맑음, 3:구름많음, 4:흐림) |
| `pty` | 강수형태 | NUMERIC | 코드값 (0:없음, 1:비, 2:비/눈, 3:눈, 5:빗방울, 6:빗방울눈날림, 7:눈날림) |
| `pop` | 강수확률 | NUMERIC | % |
| `wav` | 파고 | NUMERIC | M (파도 높이) |
| `pcp` | 1시간 강수량 | TEXT | mm (범주: 없음, 1mm 미만, 1~29mm, 30~50mm, 50mm 이상) |
| `reh` | 습도 | NUMERIC | % |
| `sno` | 1시간 신적설 | TEXT | cm (범주) |
| `updated_at` | 수정시각(UTC) | TIMESTAMPTZ | 데이터 수정 시각 (UTC) |
| `updated_at_kr` | 수정시각(KST) | TIMESTAMPTZ | 데이터 수정 시각 (KST) |

**발표 주기**: 매일 02, 05, 08, 11, 14, 17, 20, 23시 (1일 8회)
**예보 기간**: 발표 시각부터 3일 후까지

---

### 2. 해양 관측 (fetch-kma-data)
**API 엔드포인트**: `https://apihub.kma.go.kr/api/typ01/url/sea_obs.php`

#### 수집 데이터 (marine_observations 테이블)

| 컬럼명 | 한국어명 | 타입 | 설명 |
|--------|----------|------|------|
| `observation_type` | 관측유형 | TEXT | 관측 데이터 유형 |
| `station_id` | 관측소ID | INTEGER | 해양 관측소 고유 번호 |
| `station_name` | 관측소명 | TEXT | 관측소 이름 (한글) |
| `observation_time_kst` | 관측시각(KST) | TEXT | KST 시각 (YYYYMMDDHHMI) |
| `observation_time_utc` | 관측시각(UTC) | TIMESTAMPTZ | UTC 시각 |
| `longitude` | 경도 | NUMERIC | 관측소 경도 |
| `latitude` | 위도 | NUMERIC | 관측소 위도 |
| **해양 기상 요소** |
| `significant_wave_height` | 유의파고 | NUMERIC | m (파도 높이) |
| `wind_direction` | 풍향 | NUMERIC | deg (풍향 각도) |
| `wind_speed` | 풍속 | NUMERIC | m/s |
| `gust_wind_speed` | 돌풍풍속 | NUMERIC | m/s |
| `water_temperature` | 수온 | NUMERIC | °C |
| `air_temperature` | 기온 | NUMERIC | °C |
| `pressure` | 기압 | NUMERIC | hPa |
| `humidity` | 습도 | NUMERIC | % |

**수집 주기**: 매 30분마다 (00분, 30분)
**데이터 특성**: 실시간 해양 관측 데이터

---

### 3. 중기예보 (get-medm-weather)
**API 엔드포인트**:
- 기온: `https://apihub.kma.go.kr/api/typ01/url/fct_afs_wc.php`
- 해상: `https://apihub.kma.go.kr/api/typ01/url/fct_afs_wo.php`

#### 수집 데이터 (medium_term_forecasts 테이블)

| 컬럼명 | 한국어명 | 타입 | 설명 |
|--------|----------|------|------|
| `reg_id` | 예보구역코드 | TEXT | 중기예보 구역 코드 (예: 12A10000) |
| `reg_sp` | 지역특성 | TEXT | A:육상, C:도시, I:해상 |
| `reg_name` | 구역명 | TEXT | 예보 구역 이름 (예: 서해북부) |
| `location_code` | 지역코드 | TEXT | 조위 관측소 코드 |
| `forecast_type` | 예보타입 | TEXT | 'marine' 또는 'temperature' |
| `stn_id` | 관측소ID | TEXT | 관측소 ID |
| `stn` | 관측소명 | TEXT | 관측소 이름 |
| `tm_fc` | 예보생성시각(UTC) | TIMESTAMPTZ | 예보 생성 시각 (UTC) |
| `tm_fc_kr` | 예보생성시각(KST) | TIMESTAMPTZ | 예보 생성 시각 (KST) |
| `tm_ef` | 예보유효시각(UTC) | TIMESTAMPTZ | 예보 유효 시각 (UTC) |
| `tm_ef_kr` | 예보유효시각(KST) | TIMESTAMPTZ | 예보 유효 시각 (KST) |
| `mod` | 모델정보 | TEXT | 예보 모델 정보 |
| `c` | 날짜구분 | TEXT | 날짜 구분 코드 |
| **기온 예보 (temperature 타입)** |
| `min_temp` | 최저기온 | NUMERIC | °C |
| `max_temp` | 최고기온 | NUMERIC | °C |
| `min_temp_l` | 최저기온하한 | NUMERIC | °C |
| `min_temp_h` | 최저기온상한 | NUMERIC | °C |
| `max_temp_l` | 최고기온하한 | NUMERIC | °C |
| `max_temp_h` | 최고기온상한 | NUMERIC | °C |
| **해상 예보 (marine 타입)** |
| `wh_a` | 파고하한 | NUMERIC | m |
| `wh_b` | 파고상한 | NUMERIC | m |
| **공통 예보 정보** |
| `sky` | 하늘상태 | TEXT | 맑음, 구름많음, 흐림 등 |
| `pre` | 강수확률 | TEXT | % |
| `wf` | 날씨 | TEXT | 날씨 상태 텍스트 |
| `rn_st` | 강수상태 | NUMERIC | 강수 상태 코드 |
| `conf` | 신뢰도 | TEXT | 예보 신뢰도 |

**발표 주기**: 1일 2회 (06시, 18시)
**예보 기간**: 발표일 기준 D+3 ~ D+10 (8일간)

---

## 📤 API로 제공하는 데이터

### get-weather-tide-data API 응답

**엔드포인트**: `/functions/v1/get-weather-tide-data`
**파라미터**:
- `code`: 지역코드 (예: DT_0001)
- `date`: 날짜 (YYYY-MM-DD)
- `time`: 시각 (HHMM, 선택사항)

#### 응답 구조

```typescript
{
  // 1. 단기예보 (D+0 ~ D+2, 3일)
  weather_forecasts: [
    {
      fcst_datetime_kr: string,      // 예보일시(KST)
      tmp: number,                    // 1시간 기온 (°C)
      tmn: number,                    // 일 최저기온 (°C)
      tmx: number,                    // 일 최고기온 (°C)
      uuu: number,                    // 풍속(동서성분) (m/s)
      vvv: number,                    // 풍속(남북성분) (m/s)
      vec: number,                    // 풍향 (deg)
      wsd: number,                    // 풍속 (m/s)
      sky: number,                    // 하늘상태 (1:맑음, 3:구름많음, 4:흐림)
      pty: number,                    // 강수형태 (0~7)
      pop: number,                    // 강수확률 (%)
      wav: number,                    // 파고 (m)
      pcp: string,                    // 1시간 강수량 (mm)
      reh: number,                    // 습도 (%)
      sno: string                     // 1시간 신적설 (cm)
    }
  ],

  // 2. 조위 데이터 (14일)
  tide_data: [
    {
      obs_date: string,               // 관측일자
      lvl1: string,                   // 간조/만조 시각1
      lvl2: string,                   // 간조/만조 시각2
      lvl3: string,                   // 간조/만조 시각3
      lvl4: string,                   // 간조/만조 시각4
      date_sun: string,               // 일출/일몰 시각
      date_moon: string,              // 월출/월몰 시각
      mool_normal: string,            // 물때(일반)
      mool7: string,                  // 물때7
      mool8: string                   // 물때8
    }
  ],

  // 3. 해양 관측 데이터 (실시간)
  marine_observations: {
    a: [  // A 관측소 (수온, 파고)
      {
        station_id: number,           // 관측소ID
        observation_time_kst: string, // 관측시각(KST)
        water_temperature: number,    // 수온 (°C)
        significant_wave_height: number // 유의파고 (m)
      }
    ],
    b: [  // B 관측소 (기온, 풍향, 풍속)
      {
        station_id: number,           // 관측소ID
        observation_time_kst: string, // 관측시각(KST)
        air_temperature: number,      // 기온 (°C)
        wind_direction: number,       // 풍향 (deg)
        wind_speed: number            // 풍속 (m/s)
      }
    ]
  },

  // 4. 중기예보 - 해상 (D+3 ~ D+10, 8일)
  marine: [
    {
      reg_id: string,                 // 예보구역코드
      reg_sp: string,                 // 지역특성 (I:해상)
      reg_name: string,               // 구역명 (예: 서해북부)
      mod: string,                    // 모델정보
      tm_fc: string,                  // 예보생성시각(UTC)
      tm_fc_kr: string,               // 예보생성시각(KST)
      tm_ef: string,                  // 예보유효시각(UTC)
      tm_ef_kr: string,               // 예보유효시각(KST)
      wh_a: number,                   // 파고하한 (m)
      wh_b: number,                   // 파고상한 (m)
      wf: string,                     // 날씨
      sky: string,                    // 하늘상태
      pre: string,                    // 강수확률 (%)
      conf: string,                   // 신뢰도
      rn_st: number,                  // 강수상태
      forecast_type: 'marine',        // 예보타입
      location_code: string           // 지역코드
    }
  ],

  // 5. 중기예보 - 기온 (D+3 ~ D+10, 8일)
  temper: [
    {
      reg_id: string,                 // 예보구역코드
      reg_sp: string,                 // 지역특성 (C:도시)
      reg_name: string,               // 구역명 (예: 인천)
      mod: string,                    // 모델정보
      tm_fc: string,                  // 예보생성시각(UTC)
      tm_fc_kr: string,               // 예보생성시각(KST)
      tm_ef: string,                  // 예보유효시각(UTC)
      tm_ef_kr: string,               // 예보유효시각(KST)
      c: string,                      // 날짜구분
      min_temp: number,               // 최저기온 (°C)
      max_temp: number,               // 최고기온 (°C)
      min_temp_l: number,             // 최저기온하한 (°C)
      min_temp_h: number,             // 최저기온상한 (°C)
      max_temp_l: number,             // 최고기온하한 (°C)
      max_temp_h: number,             // 최고기온상한 (°C)
      sky: string,                    // 하늘상태
      pre: string,                    // 강수확률 (%)
      conf: string,                   // 신뢰도
      wf: string,                     // 날씨
      rn_st: number,                  // 강수상태
      forecast_type: 'temperature',   // 예보타입
      location_code: string           // 지역코드
    }
  ],

  // 6. WeatherAPI 시간별 예보 (14일, 336시간)
  weatherapi: {
    hourly: [
      {
        observation_time_local: string, // 관측시각(현지시각)
        forecast_date: string,          // 예보날짜
        forecast_time: string,          // 예보시각
        condition_text: string,         // 날씨상태(텍스트)
        condition_code: number,         // 날씨상태(코드)
        temp_c: number,                 // 기온 (°C)
        wind_mph: number,               // 풍속 (mph)
        wind_kph: number,               // 풍속 (kph)
        wind_degree: number,            // 풍향 (deg)
        wind_direction: string,         // 풍향 (방위)
        gust_mph: number,               // 돌풍풍속 (mph)
        gust_kph: number,               // 돌풍풍속 (kph)
        data_type: 'forecast'           // 데이터타입
      }
    ]
  }
}
```

---

## 📊 데이터 흐름 요약

```
┌──────────────────────────────────────────────────────┐
│ 기상청 API (KMA)                                      │
├──────────────────────────────────────────────────────┤
│ 1. 단기예보 (8회/일)        → weather_forecasts       │
│ 2. 해양관측 (48회/일)       → marine_observations     │
│ 3. 중기예보-기온 (2회/일)   → medium_term_forecasts  │
│ 4. 중기예보-해상 (2회/일)   → medium_term_forecasts  │
└──────────────────────────────────────────────────────┘
                          ↓
┌──────────────────────────────────────────────────────┐
│ Supabase Database                                     │
│ + WeatherAPI 데이터                                   │
└──────────────────────────────────────────────────────┘
                          ↓
┌──────────────────────────────────────────────────────┐
│ get-weather-tide-data API                            │
│ (통합 응답: 단기+중기+조위+해양관측+WeatherAPI)         │
└──────────────────────────────────────────────────────┘
```

## 🎯 주요 특징

1. **시간대 처리**: 모든 시간은 UTC와 KST 양쪽 저장
2. **중복 방지**: location_code 기반 UPSERT
3. **예보 기간**:
   - 단기예보: D+0 ~ D+2 (3일)
   - 중기예보: D+3 ~ D+10 (8일)
   - 총 11일 예보 제공
4. **실시간 관측**: 해양 관측 데이터는 30분마다 수집
5. **다층 데이터**: 단일 API로 5개 데이터 소스 통합 제공
